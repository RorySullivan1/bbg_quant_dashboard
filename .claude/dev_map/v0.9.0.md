# v0.9.0 — Single Strategy Tab

> **Status:** Planned. Documentation only — the roadmap the v0.9.0 cycle is
> executed against.
> **Depends on:** v0.6.0 (`layout/` package, templates), v0.6.5 (dark theme),
> and **v0.7.0** (the equity-risk-premium / term-premium factor series, needed
> by Section 3's factor scatter).
> **Theme:** add a third top-level tab — **Single Strategy** — a focused
> per-strategy deep-dive alongside Platform and Multi-Strategy Analysis.

## 1. Summary & non-goals

A new **Single Strategy** tab with a single-select strategy picker and one
**shared benchmark selector**, organized into three sections:

1. **Profile + cumulative chart** (two columns).
2. **Tabbed monthly-return calendar table** (absolute / outperformance /
   vol-adjusted).
3. **Tabbed analytics charts** (weekly-vs-benchmark scatter, histogram vs
   benchmark, monthly factor scatter).

**Non-goals (hard constraints):**

- **Platform and Multi-Strategy Analysis tabs are unchanged.**
- **No extra BQL call** — benchmarks already ride the single fetch; every view
  computes from the cached `universe_prices` / `arp_universe_prices`.
- **No new runtime deps;** render via v0.6.0 HTML templates; colors/sizes from
  `style.py` tokens.

## 2. Motivation

The dashboard has whole-catalog (Platform) and multi-strategy comparison
views, but **no single-strategy deep-dive**. Clients want one place to see a
strategy's profile, its monthly return calendar, and its behavior versus a
benchmark and the macro premia.

## 3. Current state (grounded, with refs)

- **Tabs:** two top-level tabs (`platform_btn`/`selected_btn`,
  `_make_tab_button` `layout.py` L172–184, `top_tab_content` W.Box L757,
  `_activate_tab` L762–771); `platform_panel`/`selected_panel` VBoxes
  L738–745. A third tab = new button + panel + 3-way swap.
- **Single-select:** none today (only multi-select `ticker_w` L497 + search
  L489) → use `W.Dropdown(options=_ticker_options(meta))` (`_ticker_options`
  L1181).
- **Cumulative chart:** `_line_chart`/`_update_line` (L1234–1260); benchmark
  columns pulled from cached `universe_prices` (cf. `_update_outperformance`
  L1029–1042). Overlay = extra `cum_perf` traces.
- **Calendar table:** **no monthly/weekly resampling in `stats.py`** — NEW.
  DataGrid + `_perf_renderers`/`VegaExpr` (L1402–1429) can render a year×month
  matrix; cell coloring needs pre-computed hex (swatch pattern) or a
  value-bound `VegaExpr`.
- **Histogram:** `_update_return_dist` (L1770–1836) already overlays series
  (`barmode="overlay"`) — reuse for strategy-vs-benchmark.
- **Scatter / regression:** `_scatter_chart`/`_update_scatter` (L1568–1642);
  `_h_ref` (L1224) draws horizontal lines only — **no OLS line today** (add a
  second `Scatter` from `np.polyfit`).
- **Factors:** `equity_risk_premium` / `term_premium` / `factor_beta` are
  **v0.7.0-planned, not yet coded**; `corr_matrix` / `rolling_correlation` /
  `ann_beta` exist.
- **`description`:** absent from `indexdb.json`. Add `"Description":
  "description"` to `COLUMN_MAP` (`data.py` L11–20) + a per-record key;
  `load_metadata` auto-pads missing columns (L49–51); metadata-only.

## 4. Workstream A — Data: the `description` field

Add `"Description": "description"` to `COLUMN_MAP`; add a `"Description"` key
to each record in `data/indexdb.json`. Metadata-only (BQL supplies only
`px_last`); `load_metadata` pads any missing record with `NA`. Update the
`CLAUDE.md` data contract to list the new field.

## 5. Workstream B — New stats (`stats.py`)

- `monthly_returns(prices)` — resample to month-end, compound daily returns.
- `weekly_returns(prices)` — resample to weekly.
- `monthly_realized_vol(returns)` — within-month daily-return std (vol-adjusted
  tab).
- `calendar_return_table(prices, *, kind, benchmark=None)` — a year×month
  matrix for `kind` ∈ {`absolute`, `outperformance` (strategy − benchmark
  monthly), `vol_adjusted` (monthly ÷ monthly realized vol)}, plus per-year
  **Return** and **Sharpe** (reuse `ann_sharpe` on each calendar year's daily
  slice).
- `ols_fit(x, y)` — slope / intercept / R² for the weekly-scatter regression
  line.
- `monthly_factor_correlations(prices, factor_returns, *, window)` — the
  strategy's per-month correlation to a factor (reuse `rolling_correlation`);
  consumes v0.7.0 `equity_risk_premium` / `term_premium`.

All new helpers get `pytest` unit tests (degenerate cases included).

## 6. Workstream C — Tab shell + Section 1 (profile + chart)

Add the **Single Strategy** tab (extend `_activate_tab` to a 3-way swap), a
single-select strategy `Dropdown`, and the shared benchmark selector. Section 1
is two columns:

- **Left — metadata card:** name, ticker, currency, return type, theme,
  category, **description** (templated; NA-safe).
- **Right:** a prominent cumulative line chart (reuse `_update_line`) + a
  compact standard-performance table (`perf_table` + `since_inception_perf`) +
  the option to **overlay one or more core benchmarks** (extra `cum_perf`
  traces driven by the shared benchmark selector).

## 7. Workstream D — Section 2 (calendar table)

A 3-tab control (Tab/pill) swapping one DataGrid between **Absolute** /
**Outperformance** / **Vol-adjusted**, each from `calendar_return_table(...)`.
Rows = years, columns = Jan…Dec **+ Year (annual return) + Sharpe** summary
columns; diverging conditional formatting on the month cells (VegaExpr /
pre-computed hex). The Outperformance tab uses the shared benchmark selector.

## 8. Workstream E — Section 3 (analytics charts, tabbed)

- **Weekly scatter:** strategy weekly returns (x) vs benchmark weekly returns
  (y) with an **OLS regression line** (`ols_fit` → second `Scatter` trace) and
  a beta / R² annotation.
- **Histogram vs benchmark:** reuse the overlay pattern — strategy + benchmark
  daily returns.
- **Factor scatter:** the **selected strategy's monthly points only** — x =
  corr to the equity risk premium, y = corr to the term premium
  (`monthly_factor_correlations`), marker color/size = that month's
  risk-adjusted return. Benchmarks may be overlaid as their own monthly point
  series; **no other strategies**. Depends on v0.7.0 factors.
  - *Definitional note:* the per-month correlation window (within-month daily
    vs a trailing rolling window sampled monthly) is a build-time choice;
    default to a trailing rolling window for stability.

## 9. Files touched (when implemented)

- `src/data.py` + `data/indexdb.json` — the `description` field.
- `src/stats.py` — `monthly_returns`, `weekly_returns`,
  `monthly_realized_vol`, `calendar_return_table`, `ols_fit`,
  `monthly_factor_correlations` (+ tests).
- `src/layout/single_strategy.py` (new) — the tab module (profile, calendar,
  charts).
- `src/layout/builder.py` — 3-way tab wiring + shared strategy/benchmark
  selectors.
- `src/layout/grids.py` — calendar grid renderer.
- `data/templates/` — profile card + calendar/section shells.
- `CLAUDE.md` — new tab, the `description` field, the data contract.

## 10. Suggested PR sequencing

Flat-named branches into the current integration branch:

1. `v0.9.0-data-description-field` (Workstream A).
2. `v0.9.0-stats-monthly-weekly-calendar` (B).
3. `v0.9.0-single-strategy-tab-and-profile` (C).
4. `v0.9.0-calendar-return-table` (D).
5. `v0.9.0-single-strategy-charts` (E).

## 11. Acceptance criteria

- A third **Single Strategy** pill appears; picking a strategy populates a
  two-column profile (metadata + description) and a cumulative chart with
  selectable core-benchmark overlays.
- The calendar table tab-switches between absolute / outperformance /
  vol-adjusted, shows years×months + Year + Sharpe columns with conditional
  coloring; the Outperformance tab responds to the shared benchmark.
- Section 3 renders the weekly-vs-benchmark scatter with a regression line, a
  strategy-vs-benchmark histogram, and the monthly factor scatter (corr ERP ×
  corr term premium).
- `description` shows in the profile and is NA-safe when missing; no extra BQL
  call; Platform / Multi-Strategy tabs unchanged.
- New stats unit-tested; `ruff`/`black`/`pytest` (from v0.6.0) stay green;
  `.meta/VERSION` → `0.9.0` at release; `CLAUDE.md` + this file updated with
  the shipped PR list.
