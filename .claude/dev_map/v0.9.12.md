# v0.9.12 — Single Strategy Filter + Code-Review & Optimization

> **Status:** In progress on the `v0.9.12` integration branch (cycle active off
> `0.9.11`). Workstream 1 (the Single Strategy "Filters" accordion) has shipped;
> Workstream 2 (the code-review & optimization pass) is underway.
> **Depends on:** v0.9.0 (the Single Strategy tab and its picker/panes), v0.6.0
> (the `layout/` package + templates) — both shipped.
> **Theme:** make the Single Strategy tab filterable and pay down review debt —
> tighten the docs, fix the CI trigger, and queue the follow-up refactors.

## 1. Summary & non-goals

Two workstreams:

1. **Single Strategy "Filters" accordion (shipped)** — a reusable filter panel
   lifted out of the Multi-Strategy tab into `src/layout/filter_panel.py`
   (`make_filter_panel`) and wired onto the Single Strategy tab, where it
   **narrows the strategy picker live** off the cache (no Refresh-prices button,
   no BQL) and auto-selects the first still-matching strategy when the picked one
   is filtered out.
2. **Code-review & optimization pass (in progress)** — a review sweep whose
   first deliverables are **documentation comprehensiveness fixes** (the repo
   memory under `.claude/context/`, the architecture map, the dev-map) and the
   **CI trigger fix** (`.github/workflows/ci.yml` triggering on the version
   integration branches rather than a single stale branch). The larger refactors
   surfaced by the review are tracked as GitHub issues tagged `v0.9.12-review`
   rather than done inline this cycle.

**Non-goals (hard constraints):**

- **No new BQL call** — the filter panel slices the already-cached
  `state.arp_universe_prices` / `state.universe_prices`, same as Multi-Strategy.
- **No behavior change to the Platform or Multi-Strategy tabs** — the
  Multi-Strategy filter was de-duplicated onto `make_filter_panel` (#155,
  Workstream 3) with its behavior preserved (same narrowing, Refresh/Clear
  controls, and selection-preserving union).
- **No new runtime dependencies.**

## 2. Motivation

The Single Strategy tab (v0.9.0) shipped with a single-select picker over the
**whole** universe — with a large catalog the user cannot narrow the picker the
way the Multi-Strategy tab's filter panel allows. Meanwhile the review found the
repo-memory docs had drifted (stale symbol names, a MultiIndex grid description
that no longer matches the flattened grids, a CI branch that no longer exists),
so the same pass tightens the docs and CI and inventories the remaining
refactors.

## 3. Current state (grounded, with refs)

- **Multi-Strategy filter UI** lives inline in `build_app`'s closures in
  `src/layout/builder.py` (pill bar over swappable checkbox groups / the
  Characteristics launch-date + currency view / the quant-threshold rows, with
  Clear section / Clear all). The Single Strategy tab (`single_strategy.py`) had
  only the single-select picker + shared benchmark selector + overlay toggle.
- **Quant-threshold slicing** reuses `quant_metrics_table` /
  `zscore_cross_section` over the cached prices (no BQL).
- **CI** (`.github/workflows/ci.yml`) triggered on a single stale integration
  branch, so newer `v*` branches were not exercised.

## 4. Workstream 1 — Single Strategy "Filters" accordion (shipped)

A **reusable "Filters" accordion** extracted into
`src/layout/filter_panel.py` (`make_filter_panel(meta)`): the pill bar (Asset
Class / Category / Theme / Return Type / Characteristics / Quantitative) over
swappable checkbox groups / the launch-date + currency Characteristics view /
the nine `≥`/`≤` quant-threshold rows, plus Clear section / Clear all.
Selection-agnostic — it exposes `.root`, `.inputs`, and `matching(meta, state)`
(the tickers passing the current filter state; categorical + Characteristics via
`apply_filters`, then the quant thresholds via the cached
`state.arp_universe_prices` / `state.universe_prices`, same `_quant_keep` logic
as Multi-Strategy). Wired onto the Single Strategy tab in `builder.py` so
toggling any filter box **re-renders the tab immediately off the cache** (no
Refresh-prices button, no BQL) and auto-selects the first still-matching
strategy when the picked one is filtered out. Tested in
`tests/test_single_strategy_filter.py`.

## 5. Workstream 2 — Code-review & optimization pass (in progress)

Deliverables this cycle:

- **Documentation comprehensiveness fixes** — correct the drifted repo-memory
  docs (`.claude/context/*`, the architecture map's `src/stats/` inventory and
  grid descriptions, the dev-map index) against the actual code.
- **CI trigger fix** — `.github/workflows/ci.yml` triggers on the version
  integration branches (`v*`) and `main`, so it can't re-drift to a single
  stale branch.

Follow-up refactors surfaced by the review are tracked as GitHub issues tagged
**`v0.9.12-review`**:

- **Multi-Strategy filter de-duplication** onto `make_filter_panel` — **✅ done
  (#155, Workstream 3):** retired the ~230-line inline copy in `builder.py`; the
  Multi-Strategy tab now composes the shared panel (Refresh button as
  `leading_actions`, its own `right_panel_layout`, `build_root=False`) and drives
  narrowing through `apply_categorical` + `quant_keep`.
- **`build_app` de-monolith** (#156) — **✅ done (Workstream 4):** extracted the
  Multi-Strategy two-pane render engine to `src/layout/multi_strategy.py` and the
  Platform-analytics render/wire orchestration into `src/layout/platform.py`
  (`wire_platform_analytics` + the `render_*` functions), both as free functions
  mirroring `single_strategy.py`. `builder.py` is down from ~1994 → ~1240 lines
  (the two render engines + the filter panel now live in their own modules).
- **Per-pane benchmark-render dedup** (#157) across the analysis panes.
- **Low-risk cleanups** (#158) identified during the sweep.

## 6. Data-layer / config changes (if any)

None — no new config constants, schema, or external inputs. The filter panel
reads existing metadata + cached prices.

## 7. Files touched (when implemented)

- `src/layout/filter_panel.py` (new) — `make_filter_panel`. ✅ (Workstream 1);
  extended with the composable `right_panel` / `apply_categorical` / `quant_keep`
  API for Multi-Strategy reuse. ✅ (Workstream 3, #155)
- `src/layout/builder.py` — wire the panel onto the Single Strategy tab ✅
  (Workstream 1); retire the inline Multi-Strategy filter and adopt the shared
  panel ✅ (Workstream 3, #155); drop the two-pane render engine + the
  Platform-analytics orchestration, calling `multi_strategy.py` / `platform.py`
  ✅ (Workstream 4, #156).
- `src/layout/multi_strategy.py` (new) — the Multi-Strategy pane render engine
  extracted from `build_app`. ✅ (Workstream 4, #156)
- `src/layout/platform.py` — added the analytics render/wire orchestration
  (`wire_platform_analytics`, `render_*`, regime helpers, `activate_platform_tab`)
  extracted from `build_app`. ✅ (Workstream 4, #156)
- `tests/test_single_strategy_filter.py` (new) — the filter accordion. ✅
- `tests/test_strategy_picker.py` — Multi-Strategy shared-panel narrowing +
  no-inline-duplication guard. ✅ (Workstream 3)
- `tests/test_lazy_views.py` / `tests/test_memo_cache.py` — retarget the
  rolling-correlation spy at `multi_strategy` (compute moved). ✅ (Workstream 4)
- `tests/test_platform.py` — Platform-analytics orchestration guards (regime
  resync + tab swap through `build_app`). ✅ (Workstream 4)
- `.github/workflows/ci.yml` — trigger on `v*` + `main`. ✅ (Workstream 2)
- `.claude/context/*`, `.claude/context/architecture.md`, `.claude/dev_map/*` —
  documentation comprehensiveness fixes. (Workstream 2)

## 8. Suggested PR sequencing

Flat-named branches into the `v0.9.12` integration branch:

- [x] **1.** `v0.9.12-single-strategy-filter` (Workstream 1) — the reusable
  `make_filter_panel` accordion wired onto the Single Strategy tab + tests.
- [ ] **2.** `v0.9.12-code-review-and-optimization` (Workstream 2) — the
  documentation fixes + CI trigger fix, with the follow-up refactors filed as
  `v0.9.12-review` issues.

## 9. Acceptance criteria

- [x] The Single Strategy tab shows a "Filters" accordion that narrows the picker
  live off the cache (no BQL, no Refresh-prices button); a filtered-out picked
  strategy auto-selects the first still-matching one.
- [ ] Docs (`.claude/context/*`, the architecture map, the dev-map) match the
  current code — no stale symbol names, grid layout, or CI branch.
- [ ] CI triggers on the version integration branches (`v*`) and `main`.
- [ ] Follow-up refactors are captured as `v0.9.12-review` issues (not dropped).
- [ ] Platform / Multi-Strategy tabs unaffected; no extra BQL call.
- [ ] Quality gates green (`ruff` / `black` / `pytest`).
- [ ] `.meta/VERSION` → `0.9.12` at release; index README + this file updated
  with the shipped PR list.
