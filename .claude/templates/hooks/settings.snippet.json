{
  "//": "Copy the hooks.PreToolUse block below into the target repo's .claude/settings.json. Both hooks receive the tool-call JSON on stdin and only act on `git commit` / `git push` respectively. Mark the .sh files executable: chmod +x .claude/hooks/*.sh",
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/.claude/hooks/quality-gates.sh"
          },
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/.claude/hooks/block-protected-push.sh"
          }
        ]
      }
    ]
  }
}
